# DnB Bass Synth MVP 0.8.0 - Tester Feedback Form

Please copy this template and fill it in for each test session.  
Language: English only.  
Version under test: `0.8.0`

## 1) Tester Information
- Tester name:
- Date (YYYY-MM-DD):
- Email/Discord (optional):

## 2) Environment
- OS + version:
- DAW + version:
- Audio interface + driver:
- Sample rate:
- Buffer size:
- Plugin format: VST3

## 3) Installation and Scan
- Plugin scan result (`PASS`/`FAIL`):
- Plugin load/open result (`PASS`/`FAIL`):
- Notes (if failed):

## 4) Functional Checks (PASS/FAIL + short note)
- MIDI input produces audio:
- No crash on load/unload:
- Style modes (`Tech/Neuro/Dark`) all work:
- Style morph control works (`macro.style_morph`):
- 4 primary macros are audible:
  - `macro.neuro_formant`
  - `macro.fm_metal`
  - `macro.roller_dynamics`
  - `macro.liquid_depth`
- 6 advanced macros are audible:
  - `macro.sub_punch`
  - `macro.fm_pressure`
  - `macro.cutoff_motion`
  - `macro.fold_bite`
  - `macro.table_drift`
  - `macro.smash_glue`
- Attack/Release parameters respond correctly:
- Host automation works without glitches:

## 5) UI/UX Checks (PASS/FAIL + short note)
- Knob behavior at exact 0% and 100% is visually correct:
- Center visual panel is reactive (no static/runtime overlap):
- Labels/readability are clear in your DAW:
- Layout usability on your screen size:

## 6) Audio Quality Checks
Rate each from 1 (poor) to 5 (excellent):
- Low-end stability:
- Style contrast clarity:
- Macro usefulness:
- Presence of clicks/glitches (inverse score: 5 means none):
- Overall sound quality:

## 7) Performance
- CPU behavior in your DAW (`Stable` / `Unstable`):
- Any spikes/dropouts? (`Yes`/`No`):
- If yes, when exactly:

## 8) Bugs / Issues Found
For each issue, use one block:
- ID: (e.g., `TEST-001`)
- Severity: `Critical` / `High` / `Medium` / `Low`
- Area: `Audio` / `UI` / `Automation` / `Preset` / `Performance` / `Other`
- Repro steps:
1.
2.
3.
- Expected result:
- Actual result:
- Reproducibility: `Always` / `Often` / `Rare` / `One-time`
- Attachment names (screenshots/audio/video), if any:

## 9) Overall Assessment
- Is this build usable for real production sketching? (`Yes`/`No`):
- Biggest strength:
- Biggest weakness:
- Top 3 improvements you want next:
1.
2.
3.

## 10) Final Recommendation
- Recommendation:
  - `Approve for MVP baseline`
  - `Approve with minor fixes`
  - `Block until critical fixes`
- Final comment:
