# Phase 6.1 Remediation Fix Handoff

**Phase ID**: P6.1 Macro-10 Expansion and Gate Closure Update
**Status**: Ready for P7 (Review)

## 1. Overview
This is the final remediation patch-set for the P6.1 Macro-10 expansion. It specifically targets findings raised during the `docs/qa/p6_1_review.md` audit, closing the remaining backend deviations to fully unlock the P7 gate.

## 2. Closure Map

| Finding ID | Title | Closure Strategy | Evidence / Changed Files |
|---|---|---|---|
| **F-P6.1-001** | FM Pressure style contract drift | Updated `ConstraintEngine.cpp` so that `macro.fm_pressure` depth actively scales based on the styling progression (`Tech < Neuro < Dark`), as opposed to being strictly muted unless Dark was active. | `src/domain/ConstraintEngine.cpp`, `tests/unit/test_macro_constraints.cpp` |
| **F-P6.1-002** | Domain safety clamp drift | Injected a final `limits::clampCutoff(out.filterCutoff)` safety-net at the exit of `ConstraintEngine::process` to guarantee `filterCutoff` always obeys global 40Hz-8kHz bounds regardless of downstream macro modulation. | `src/domain/ConstraintEngine.cpp`, `src/domain/DnBLimits.h` |
| **G-P6P7-003** | Single source plugin version | Removed the hardcoded `"0.1.0"` from `PresetSchema.h`. Instead, the file `#include "Config.h"`, drawing `pluginVersion` dynamically generated by CMake via `Config.h.in`, establishing a true single source of truth linked to `PROJECT_VERSION`. | `src/preset/PresetSchema.h`, `src/Config.h.in`, `CMakeLists.txt` |
| **F-P6.1-004** + **G-P6P7-002** | Evidence Integrity | Updated `docs/qa/backend_phase_handoffs/P6_handoff.md` test run syntax (`ctest -R preset` -> `ctest -R Preset`) and aligned wrapper wording with implementation (`DnBBassSynthState` XML payload embedded in JSON state envelope). | `docs/qa/backend_phase_handoffs/P6_handoff.md` |

## 3. Test Evidence

```text
Test project /Users/grzzi/Desktop/dnb_bass_synth/build
    Start 36: PresetManager graceful handling of corrupt XML/data
1/5 Test #36: PresetManager graceful handling of corrupt XML/data ..............   Passed    0.08 sec
    Start 37: PresetManager gracefully ignores unknown fields and parameters
2/5 Test #37: PresetManager gracefully ignores unknown fields and parameters ...   Passed    0.08 sec
    Start 38: PresetMigration Legacy Up-conversion
3/5 Test #38: PresetMigration Legacy Up-conversion .............................   Passed    0.08 sec
    Start 39: PresetManager saveState outputs canonical plugin version
4/5 Test #39: PresetManager saveState outputs canonical plugin version .........   Passed    0.08 sec
    Start 40: PresetManager Save and Load Roundtrip
5/5 Test #40: PresetManager Save and Load Roundtrip ............................   Passed    0.08 sec

100% tests passed, 0 tests failed out of 5
```

```text
    Start  3: Macro changes respect constraints engine constraints
 3/40 Test  #3: Macro changes respect constraints engine constraints .............   Passed    0.00 sec
    Start  4: Style modes restrict unaccepted parameter modifications
 4/40 Test  #4: Style modes restrict unaccepted parameter modifications ..........   Passed    0.00 sec
...
100% tests passed, 0 tests failed out of 40
```

*Note: All 40 domain and runtime effect tests ran green, confirming the `Tech < Neuro < Dark` mapping passes bounds successfully.*

## 4. Open Questions / Next Steps
No technical backend gaps remain. With all pre-P7 gate deviations and P6.1 domain issues formally closed, the integration of real UI macro inputs to the APVTS listeners defined in the backend can safely commence. Handing off to P7.
